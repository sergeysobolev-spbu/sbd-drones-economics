# Components - модули дрона, запускаемые как отдельные процессы (python -m components).
# Используют SystemBus (единая шина с системами).
# Тип компонента выводится из COMPONENT_ID (например dummy_component_a → dummy_component).
