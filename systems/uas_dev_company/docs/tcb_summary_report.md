# Сводный отчёт ДВБ (последний `make tcb-test`)

- **Сформирован:** 2026-05-07 12:52:47 UTC
- **Cobertura:** `docs/tcb_coverage.xml`
- **Метрики:** `docs/tcb_metrics.json` (`tcb_cost_task12`, `tcb_ipc_topology_task14`)

## Общее покрытие измеренных строк (все файлы в отчёте coverage)

**71.1%** строк с ненулевым hit (1282 / 1804).

## Домены безопасности: покрытие, IPC, цели безопасности

Уникальных **ориентированных** связей между доменами (по allow-политикам, без умножения на действие): **26**. Методика — `methodology_ru` в `tcb_metrics.json` → `tcb_ipc_topology_task14`.

| Сервис compose | Покрытие | Покрыто / всего | Входящие IPC (число соседей) | Исходящие IPC | Влияние на цели безопасности | Назначение (кратко) |
|---|---:|---:|---:|---:|---|---|
| `api_gateway` | 74.0% | 852 / 1152 | 1 | 7 | ТБ: точка входа, привязка к ролям разработчика (ЦБ-2) для операций сертификации и реестра; ТБ: маршрутизация внутреннего IPC под политиками монитора (реализация всех трёх ЦБ) | HTTP API, JWT, прокси к доменам реализации ЦБ-1…ЦБ-3 через security_monitor |
| `security_monitor` | 76.4% | 603 / 789 | 7 | 7 | ТБ: разграничение маршрутов IPC к воркерам ЦБ-1…ЦБ-3; ТБ: явные политики запроса и ответа по доменам (ipc_inbound_request / ipc_response) | Единая точка allow/deny для внутреннего РКИВ (контроль доступа к ЦБ-операциям) |
| `user_management_worker` | 74.9% | 670 / 894 | 2 | 2 | ЦБ-2: только авторизованный разработчик может запускать операции сертификации и реестра; ТБ: учётные записи, хэши паролей, роли (механизм достижения ЦБ-2) | ЦБ-2: идентификация разработчика, инициирующего сертификацию и регистрацию |
| `firmware_ingestion_worker` | 75.3% | 623 / 827 | 2 | 2 | ЦБ-1: фиксация подлинности и происхождения прошивки до решения о сертификации; ТБ: метаданные поставки, неизменяемые идентификаторы артефакта | ЦБ-1: приём только проверяемого артефакта прошивки (хеш или репозиторий+коммит) |
| `certification_service_worker` | 76.5% | 708 / 925 | 2 | 2 | ЦБ-1: связка прошивки с сертификатом; отказ при подделке целей или статуса; ТБ: сценарии отзыва и сужения заявленных целей (effective_security_goals) | ЦБ-1: перевод прошивки в сертифицированный контур по решению Регулятора |
| `drone_registry_worker` | 75.6% | 689 / 911 | 2 | 2 | ЦБ-3: допуск в реестр/витрину по инвариантам сертификата и регистрации; ТБ: согласование заявленных целей экземпляра с сертификатом (множество ЦБ-1…ЦБ-3, ТБ-2) | ЦБ-3: витрина и карточка экземпляра только при действующей сертификации и регист… |
| `purchase_service_worker` | 76.1% | 667 / 877 | 2 | 2 | ТБ-1: исключение повторной продажи, согласованность статуса владения; ТБ-3: фиксация передачи до разрешённой эксплуатации у Эксплуатанта | ТБ-1, ТБ-3: сделка, перерегистрация владения (вне официальных ЦБ-1…ЦБ-3) |
| `audit_log_worker` | 75.6% | 643 / 850 | 7 | 2 | ТБ: аудируемость операций для расследований и демонстрации контроля; ТБ: полезная нагрузка событий без утечки избыточных данных (journal_policy) | ТБ: системный журнал (SQLite) и при DRONE_ANALYTICS_ENABLED — дублирование в ana… |
| `analytics_adapter_worker` | 73.1% | 651 / 891 | 1 | 0 | ТБ: изоляция сетевого исхода к системным.drone_analytics от доменов ЦБ; ТБ: приём только доверенных отправителей (домены ЦБ и audit_log) на IPC | ТБ: граница доставки во внешний журнал DroneAnalytics (отдельный контур от audit… |

Строки **общего ядра `shared`** входят в объём каждого домена (как в `python_path_specs`); проценты по строкам просуммированы по всем файлам спецификации домена. IPC: соседи по дедуплицированным парам доменов из политик (`ipc_inbound_request`, ответы `ipc_response`, запросы шлюза).

## Оценка стоимости текущей реализации ДВБ (Задача 12)

- **`estimated_tcb_cost_score`:** 68.03
- **`allow_rules_count`:** 35
- **`union_backend_python_sloc` (объединённый backend):** 2789
- **Шлюз импортирует домены в `server.py`:** False

Формула и трактовка: поле `formula_ru` в `tcb_cost_task12`, текст — [`docs/tcb_assessment.md`](../tcb_assessment.md).
