# Запросы на изменение

## Запрос 1

учти, что названия топиков всех внешних систем содержат уникальные идентификаторы этих систем (кроме Регулятора) и должны запрашиваться у Регулятора. 

Добавь запрос топиков от Регулятора и возьми ответ из переменных окружения в тестовых целях. 

Также учти, что Эксплуатант покупает БАС у сертифицированных Разработчиков. Добавь запросы на список доступных БАС от всех Разработчиков и покупку БАС с учётом стоимости и целей безопасности. 

В тестовых целях ответ Разработчиков пока возьми из yaml файла, имя которого будет в переменных окружения. 

Сгенерируй yaml файл с примером ожидаемого ответа Разработчиков, используй в интеграционных тестах. На основе этой логики сформируй парк БАС.

Используй описанную в спецификации структуру папок для компонентов и тестов для них, в качесте примера используй components/dummy_component. Используй структуру каталогов для тестов из спецификации (tests/unit, tests/module, tests/integration)

Переработай код и тесты. 

Добавь Jupyter notebook с описанием реализации Эксплуатанта и демонстрацией функционала. Также в блокноте покажи работу монитора безопасности. 

Для диаграмм используй plantuml, сгенерируй png файлы и вставь в md документацию.

Для подготовки и запуска тестов создай и используй виртуальное окружение с помощью pipenv

## Запрос 2

* Учти, что, согласно спецификации, различные данные и прочие ресурсы системы должны храниться в каталоге resources использующего эти данные компонента. 

* Переработай структуру папок исходного кода для компонентов согласно спецификации и примеру components/dummy_component и systems/dummy_system. 
Перемести тесты компонента внутрь структуры папок этого компонента согласно спецификации, исходи из концепции "системы систем", где одинаковые принципы применяются на всех уровнях абстракции. 

* Для каждого компонента подготовь документацию и цели безопасности в терминах ГОСТ 72118. Обеспечь консистентность целей безопасности компонентов и систем. Добавь таблицу трассировки и обоснование целей безопасности компонентов.

* Минимизируй количество, сложность кода и количество интерфейсов в доверенных доменах безопасности. Максимально возможное количество некритичного для целей безопасности кода перенеси в недоверенные домены. Обоснуй выбранное решение в документации.

* Для системы Эксплуатанта, состоящей из нескольких компонентов, оставь формулировки целей безопасности в терминах кибериммунной разработки.

* Учти, что цели безопасности каждой системы по-английски должны называться security goals. В реестре систем они могут использоваться в комбинации с safety features, но не заменять их. Примеры целей безопасности приведены в описании концепции.

* Добавь Регулятору функцию ведения реестра типовых целей безопасности с идентификаторами, а в реестре систем используй идентификаторы целей безопасности вместо полного текста. 
Добавь в ресурсы ответ Регулятора с описанием типовых ЦБ и их идентификаторов. Используй идентификаторы цб в тестах.

* Переработай Makefile, чтобы настройка зависимостей и все тесты выполнялись в виртуальном окружении с использованием pipenv

* Учти, что в системе используется python 3.12.3

* Проанализируй зависимости для файлов из папки sdk и обнови requirements.txt

* Сейчас тесты и демонстрации завершаются с ошибкой. Переработай код, конфигурационные файлы,  тесты и демонстрации с учётом всех вводных, без ухудшения качества тестирования.  Проверь, что тесты и демонстрации выполняются без ошибок.

* добавь интеграционный тест с запуском docker контейнеров, используй docker-compose.yml, убедись, что тесты проходят без ошибок. Добавь проверки healthcheck контейнеров компонентов.

* обеспечь правильную работу всех целей в Makefile, выполни контрольный запуск и устрани ошибки

* Добавь диаграммы последовательности для описания взаимодействия Эксплуатанта с другими системами в документацию. Используй plantuml. Добавь генерацию диаграмм в png из plantuml в Makefile

* Обнови jupyter notebook с примерами - добавь описание домена (контекста) и диаграммы о работе Эксплуатанта. Добавь в блокнот диаграммы. Доработай код, убедись, что все ячейки выполняются без ошибок.

## Запрос 3

* учти, что все системы и компоненты будут в тестовом окружении использовать один брокер, поэтому в названии топиков компонентов используй уникальный идентификатор системы, частями которой эти компоненты являются. Идентификатор будет задаваться переменной окружения. Добавь версионирование для названия внешних топиков системы (используемых для взаимодействия с другимим системами).

* в духе компонента fleet_manager переработай все остальные компоненты Эксплуатанта, удали неиспользуемые в проекте файлы в папках systems/operator/src, systems/operator/data, systems/operator/tests

* добавь конфигурационный файл pytest.ini, настрой PYTHONPATH, чтобы импорты между компонентами работали правильно

* обеспечь правильное прохождение локально запущенных тестов (с помощью make test-*)

* обеспечь выполнение требований к системе Эксплуатанта, описанных в файле design.md

* учитывай общие требования к системе Эксплуатант, описанные в docs/requirements_spec.md

* продумай и обеспечь сквозную трассировку событий в системах и компонентах, используй trace_id, span_id, при необходимости - parent_span_id. Добавь описание логики журналирования в документацию.

* актуализируй Makefile и docker-compose файлы в каталоге systems/operator, обеспечь успешное выполнение интеграционных тестов для системы Эксплуатант командой make test-integration в каталоге systems/operator

* jupyter блокнот systems/operator/notebooks/uas_purchase_demo.ipynb не был обновлён, он не соответствует текущей реализации и при выполнении выдаёт ошибки импортов. Переработай этот блокнот, также добавь в него диаграммы с описанием логики тестируемых сценариев.

* в каталоге systems/operator/tests в соответствующих подкаталогах должны остаться интеграционные и сквозные тесты уровня всей системы, актуализируй содержимое каталогов

C учётом изменений переработай код и тесты, обнови документацию, выполни тесты и демонстрации, обеспечь успешное прохождение. 

В документации, включая planutml диаграммы, используй русский язык. Проверяй читаемость всех символов.

## Запрос 4

* большинство компонентов используют одинаковые зависимости, сделай базовый docker образ с максимальным количеством общих зависимостей, используй его как основу для всех компонентов. 
Используй версию python 3.12.3 (начальный образ python:3.12.3-slim)

* переработай Dockerfile всех компонентов, чтобы они использовали новый базовый образ

* переработай оставшиеся монолитные компоненты в файлах systems/operator/src/*.py по аналогии с systems/operator/business_logic согласно требованиям к компонентам в docs/requirements_spec.md, разработай для них юнит и модульные тесты, используй ранее созданную структуру папок для перерабатываемых компонентов

## Запрос 5

Добавь дополнительный компонент "журнал событий" в систему Эксплуатант, все компоненты, включая монитор безопасности, должны передавать ему события через брокер, он будет отправлять события в центральный журнал. Используй типы и уровни важности событий, записываемых в журнал.

Переработай логику ответов через брокер, чтобы они тоже проверялись монитором безопасности, как и запросы. При этом убери проверки на соответствие политикам безопасности "внешние" запросы - т.е. запрос от внешних систем, потому что встроенный монитор безопасности Эксплуатанта проверяет только внутренние запросы и ответы (IPC).

Учти, что файлы в каталоге sdk используются многими системами, а не только Эксплуатантом. и нельзя вставлять прямые зависимости в sdk на специфические компоненты отдельных систем, следуй SOLID принципам и при необходимости реализуй dependency injection.

Реализация центрального журнала находится в каталоге systems/analytics, проанализируй код и API, продумай внутренний адаптер для Эксплуатанта, интеграционные тесты и скрипты.

Добавь журналирование во всех компонентах для отладки, в том числе приём и отправку всех сообщений через брокер.

Проработай интеграцию системы Эксплуатант с системой Агрегатор, находящейся в каталоге systems/agregator

Проработай интеграцию системы Эксплуатант с системой Страховщик, находящейся в каталоге systems/insurer. 

Проработай интеграцию с наземной управляющей станцией (НУС), реализованной в каталоге systems/DronePortGCS/systems/gcs. Главное назначение НУС - это расчёт перемещения дрона для выполнения задания, система Эксплуатант передаёт ей подтверждённое задание от Агрегатора, а НУС должна подготовить миссию для регистрации в ОрВД БАС.

Учти, что сейчас появилась реализация системы Агродрон (в каталоге systems/cyber_drons/agrodron), который должен будет взаимодействовать с НУС и Дронопортом. Реализация Дронопорта пока не готова. Однако нужно в тестах реализовать заказ для агро-дрона, который будет выполняться реализацией системы из systems/cyber_drons/agrodron. Изучи реализацию системы Агродрон и при необходимости адаптируй тестовые данные.

Проработай изменения в скриптах для запуска и интеграционные тесты. 

После изменений выполни все тесты (юнит, модульные, интеграционные, shell, для kafka и mqtt, безопасности, coverage), устрани ошибки.

Учти, что в текущей версии система Агрегатор работает только с Kafka, не поддерживает Mosquitto, поэтому интеграционные тесты с Агрегатором проводи с Kafa, а для тестов с mqtt используй совместимые по интерфейсу (названиям топиков и формату сообщений) заглушки.

Выполни демо-цели в Makefile, проверь, что они выполняются без ошибок.

Добавь интеграционные (в tests/integration) и сквозные тесты (в tests/e2e), доработй общий Makefile для запуска всех систем из корня проекта, демо-режим, автотесты, по аналогии с Makefile Эксплуатанта пропиши запуск всех зависимостей для успешного выполнения тестов одной командой (например make tests, make tests-integration, make tests-e2e) без необходимости отдельно запускать инфраструктуру брокера или журнала.

Учти описание структуры тестов согласно спецификации /home/user/projects/sbd-drones-economics/sbd-drones-economics-ai/docs/requirements_spec.md

Продумай необходимые доработки в смежных системах, если это критично для интеграции.

Подготовь новый jupyter блокнот, демонстрирующий работу связки. 