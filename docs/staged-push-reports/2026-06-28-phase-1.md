# Staged push — Phase 1 (`sbd-drones-economics`, PR-E1)

<!-- doc-meta: status=active version=1.0 updated=2026-06-28 -->

**Дата:** 2026-06-28  
**Ветка:** `feature/uas-dev-company` → `master` (PR-E1)  
**Коммиты относительно локального `master`:** 34 (FF от `master` возможен локально); относительно **`origin/master`:** 11 впереди, **5 позади** — **FF в `master` на remote невозможен** без предварительного merge/rebase с `origin/master`.

## Ворота (до push)

| Команда | Результат | Примечание |
|---------|-----------|------------|
| `make ci-test` | **GREEN** (exit 0) | Требует доступ к Docker; в sandbox без `docker.sock` падает `systems/Agregator` integration-test |
| `make e2e-codespace` | **GREEN** (29 passed, 1 skipped, ~5 min) | Предварительно остановлен конфликтующий `agregator-aggregator-1`, удалена сеть `drones_net` с неверной compose-меткой |

## DevOps (`ci-marinet-steward`)

- **CI/Makefile:** затронуты `CI_UNIT_EXCLUDE`, `ci-integration-test` exclusions, цель `e2e-codespace` (`E2E_SKIP_ANALYTICS=1`, warmup, `prepare_multi.py`).
- **Jenkins:** JCasC job без изменений в этом срезе; root `Jenkinsfile`/GitHub Actions на `origin/master` **опережают** feature (5 коммитов) — риск конфликта при merge PR-E1.
- **Compose/ports:** E2E использует `.generated/e2e/`; коллизия внешней сети `drones_net` — операционный риск параллельных стеков.
- **Push:** `feature/uas-dev-company` уже **совпадает** с `origin/feature/uas-dev-company` (`9cc303a`).

## QA (`qa-marinet-spec`)

- Unit/integration: uas_dev_company 17 passed, 11 skipped (docker-live и bus-mock — осознанные skip).
- E2E: 1 skip (`test_events_present_in_analytics`) при `E2E_SKIP_ANALYTICS=1` — соответствует политике codespace-gate.
- **Аdequacy:** gate зелёный, но skip-policy E2E не доказывает полный analytics-path (зафиксировано в `ai_dev_tasks`).

## Техпис (`documentation-governance`)

- Документы uas_dev_company quickstart/README в срезе feature; термин **СКИБ** — в учебных материалах `-ai`, не в этом push.
- PII в коммитах feature не выявлено (диаграммы/стек uas_dev_company).

## Действия фазы

| Действие | Статус |
|----------|--------|
| Push `feature/uas-dev-company` | **Не требуется** (already up to date) |
| FF merge → `master` + push | **BLOCKED** — нет FF к `origin/master` (divergence 11/5) |

## Следующий шаг

Rebase или merge `origin/master` в `feature/uas-dev-company`, повторить `make ci-test` + `make e2e-codespace`, затем merge PR-E1 (не force-push `master`).
